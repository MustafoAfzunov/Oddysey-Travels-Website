{
  "name": "ravsh-website-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix \"./front-end\" run dev",
    "build": "npm --prefix \"./front-end\" run build",
    "preview": "npm --prefix \"./front-end\" run preview"
  }
}
